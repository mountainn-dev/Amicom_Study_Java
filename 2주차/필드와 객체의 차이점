필드 : 클래스나 객체의 속성을 나타냄

해당 클래스나 객체의 모든 메소들에서 사용가능 --- 변수는 메소드 내에서만 사용 가능

객체의 상태를 저장하는데 사용,객체 생성 후에도 유지 --- 변수는 메소드 호출떄마다 새로운 값 할당, 종료시 사라짐

기본값으로 초기화(지정X시 기본값 설정) --- 변수는 초기화하지 않은 상태에서 사용시 컴파일 에러 ㅏㅂㄹ생


=== 필드는 객체의 속성을 저장, 변수는 메소드 내에서 임시적인 값을 저장
